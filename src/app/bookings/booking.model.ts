export class Booking {
    id: string;
    placeId: string;
    firstName: string;
    lastName: string; 
    // userId: string;
    guests: number;
    checkIn: Date;
    nights: number;
    totalPrice: number;

    constructor(

        // public price: number 
    ) {}
}